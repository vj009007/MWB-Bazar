import { css } from "@emotion/css";

export const useMuitreeViewStyles = () => ({
  root: css`
    padding-top: 30px;
    padding-bottom: 30px;

    .treefont {
      font-size: 16px !important;
      font-family: "Manrope", serif !important;
    }
  `,
});
